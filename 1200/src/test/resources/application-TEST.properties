service.hello.url=http://localhost:${wiremock.server.port}
spring.main.allow-bean-definition-overriding=true
queue.type=activemq-test
queue.connection=vm://localhost?broker.persistent=false
queue.name=foo
management.server.port=0
